let numero = parseInt(prompt("Escolha o numero: "))

    for(let multiplicador = 0; multiplicador <= 20; multiplicador++) {
        console.log(`${numero} x ${multiplicador} = ${numero * multiplicador}\n`)
    }
